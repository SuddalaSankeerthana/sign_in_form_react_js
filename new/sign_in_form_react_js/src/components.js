
export default function UserData() {
    return (
      <section>
        <table>
        const renderList = UserData.map((item, index) =
                             <div key={index}>{item}</div>
                           )
        </table>
      </section>
    );
  }